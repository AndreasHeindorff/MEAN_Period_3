------------------------------------------------------------------------------------------------------------------------
4. Explain reasons to add a layer like Mongoose, on top on of a schema-less database like MongoDB.
------------------------------------------------------------------------------------------------------------------------
MongoDB is a schema-less database and that makes things kinda abstract. Mongoose adds a layer on top of mongoDB
that allows you to add a model to your database. Mongoose doesn't add new functionality to your database but if
a more structured database is needed it is essential to use.